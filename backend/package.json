{
  "name": "casting-agency",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "init": "pipenv run python init_data.py",
    "server": "pipenv run flask run",
    "db-init": "pipenv run flask db init",
    "db-migrate": "pipenv run flask db migrate",
    "db-upgrade": "pipenv run flask db upgrade",
    "db-downgrade": "pipenv run flask db downgrade",
    "create-test": "sudo -u postgres -i && pg_dump agency > agency.psql",
    "test-db": "dropdb agency_test --if-exists && createdb agency_test && psql agency_test < agency.psql",
    "test": "pipenv run nose2 -v -C",
    "test-full": "npm run test-db && npm run test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
